name: Hello World

on:
  push:
    branches: [ test ]

jobs:
  hello:
    runs-on: test
    steps:
      # ✅ Клонирует (checkout) текущий репозиторий
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: my-folder

      # ✅ Создаёт папку my-folder
      - name: Git pull
        run: docker

      # # ✅ Копирует все файлы из репозитория в эту папку
      # - name: Copy code to folder
      #   run: cp -r * my-folder/

      # # ✅ Выводит содержимое созданной папки
      # - name: List folder contents
      #   run: ls -la my-folder
